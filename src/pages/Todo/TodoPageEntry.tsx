// This fetches a todolist
interface TodoPageEntryProps {
  //  Which has an id
  todolistId: string;
}
export default function TodoPageEntry(props: TodoPageEntryProps) {
  // Fetches data and makes subscription on high level.
}
